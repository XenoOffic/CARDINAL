[package]
name = "cardinal-core"
version = "0.1.0"
edition = "2021"
description = "Native Rust core for the CARDINAL runtime"
license = "MIT"

[lib]
name = "cardinal_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
